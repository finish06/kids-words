# Kids Words — Staging Environment
# Copy to .env and fill in values on staging1

# Database
POSTGRES_USER=kids_words
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=kids_words

# Image tag (set by deploy hook, or manually)
IMAGE_TAG=beta-latest

# Registry auth (for docker compose pull)
# Run: docker login dockerhub.calebdunn.tech -u finish06
